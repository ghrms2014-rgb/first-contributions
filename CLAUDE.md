# 이 저장소에 대해

호주 시드니 거주 · 호주 시민권자인 운영자의 **수익화 프로젝트**입니다.
원래 first-contributions 튜토리얼 저장소였고, 작업물은 아래 두 폴더에만 있습니다.

## 운영자 배경 (작업 시 반드시 반영)

- 시드니 거주, 호주 시민권자. **한국 국적 없음.**
  - 쿠팡파트너스 등 한국 전용 서비스는 제안하지 말 것
  - 광고 표기는 호주소비자법(ACL)·ACCC·AANA 기준
  - 세무는 ABN / GST A$75,000 / W-8BEN 기준. 조언이 아님을 항상 명시
  - 시간대는 시드니 (AEST UTC+10 / AEDT UTC+11). Actions cron은 UTC 고정
- 직업: **시드니 건설현장 Site Safety Coordinator.** 삽질부터 시작해 올라옴
- 코딩은 거의 모름. **설정 파일만 고치면 되도록** 만들 것
- 가용 시간은 주말 위주, **운영 비용 0원**이 전제 (외부 패키지 금지, 표준 라이브러리만)

## 인스타그램 @hokeun_downunder — 현재 상태

| 지표 | 값 | 해석 |
| --- | --- | --- |
| 팔로워 | 675 | 게시물 552개 대비 매우 낮음 |
| 30일 조회 | 97,000 | 도달은 충분함 |
| 팔로우/1k 도달 | **0.33** | 심각. 목표는 10 이상 |

**핵심 진단:** 문제는 도달이 아니라 전환이다. 대표 릴스
"호주숲에 함부로 들어가면 안 되는 이유"가 45,280명에게 닿아 15명만
팔로우했다. 호기심으로 퍼졌을 뿐 타깃(호주 건설 취업 희망자)에게
닿지 않았다는 뜻이다.

**따라서 조회수를 목표로 삼지 말 것.** 계정 계정성이 컨스트럭션·가족·운동
셋으로 갈려 있어 알고리즘이 대상을 특정하지 못하는 상태이기도 하다.

## 폴더

### `content-engine/` — 릴스 소재 + 뉴스레터

```bash
python3 content-engine/run.py        # 수집 -> 소재 -> 초안 -> 사이트
python3 content-engine/selftest.py   # 인터넷 없이 도는 검사 (43건)
python3 content-engine/ideas.py      # 릴스 소재만
```

- `config.json` — 운영자가 고치는 유일한 파일. `presets/` 에서 갈아끼움
- `angles.json` (앵글 10종) × `evergreen.json` (상시 주제 50개) = 조합 295개
  - 분류마다 성립하는 앵글만 붙는다. 이 제약을 지울 것
  - `react`(뉴스 반응)는 `news_only`. 상시 주제에 붙이면 안 됨
- `ideas/bank.json` — 사용/성과 기록. **성과는 조회수가 아니라
  도달 1,000명당 팔로우로 잰다.** 이 지표가 이 프로젝트의 중심이다

```bash
python3 content-engine/ideas.py --used <키> --reach 45280 --follows 15 --saves 7
python3 content-engine/ideas.py --stats
```

### `creator-hub/` — 인스타 프로필 링크가 향할 허브

```bash
python3 creator-hub/build.py   # -> _site/
```

`profile.json` 만 고치면 됨. 최상단 CTA는 **이메일 수집** 하나로 유지할 것.
팔로워는 인스타가 소유하지만 이메일은 운영자가 소유한다.

## 회사 촬영 제약 (2026-08 회사 답변) — 최우선

운영자는 **현직 Site Safety Coordinator**다. 회사로부터 로고·현장 노출
불가 답변을 받았다. 계정보다 직업이 우선이므로 이 제약이 콘텐츠 기획의
가장 상위 조건이다.

**금지:** 회사 로고(헬멧·조끼·차량·서류·출입증), 현장 영상/사진(배경 포함),
회사명·프로젝트명·고객사명·동료, **본인 회사의 실제 사고·징계 사례**
(회사 문제를 넘어 법적 위험 — 산재는 조사 대상이고 관련자는 민감정보)

**가능 (2026-08 운영자가 회사에 확인함):** **직함 사용(Site Safety
Coordinator)**, **사복 차림으로 업계 일반론 말하기**, 집·차 안(주차)·카페·
공원 촬영, 로고 없는 본인 장비, 공개 자료 화면 녹화(SafeWork NSW 등),
손글씨·화이트보드, 가린 서류, 본인 경험의 일반화("호주 현장에서는 보통…")

즉 제약은 **로고·현장·회사 고유 정보**에 한정된다. 직함의 권위는 그대로
쓸 수 있으므로 포지셔닝을 바꿀 필요가 없다.

기계적으로는 `evergreen.json` 의 `filming_rules`(소재 시트 상단에 출력)와
`cautions`(위험 주제에 개별 경고)로 강제된다. 새 주제·앵글을 추가할 때
현장 촬영을 전제하지 말 것.

**이 제약은 불리하지 않다.** 볼거리가 강했던 "호주숲" 릴스의 전환이
0.33으로 최하위였다. 정보 니치에서는 토킹헤드+자막이 전환이 더 높다.

## 규제 — 넘으면 안 되는 선

- **이민 조언은 MARA 등록 대리인만 가능.** 경험 공유·뉴스 요약은 되지만
  개인별 비자 상담은 안 됨. 허브 페이지에 고지 문구가 고정되어 있으니 지울 것
- **금융상품 조언은 AFS 라이선스 필요** (ASIC INFO 269). 주식·투자 콘텐츠는 위험
- 제휴 링크는 광고 표기 필수

## 배포

- Pages는 **Actions 방식** (`.github/workflows/pages.yml`).
  브랜치 배포는 루트나 `/docs` 만 되는데 `/docs` 는 이미 사용 중이라 불가
- `content-engine.yml` 이 매일 UTC 20:00 (시드니 아침) 실행
- 작업 브랜치: `claude/money-earning-methods-e9k2kd`

## 작업 원칙

1. 외부 패키지를 추가하지 말 것. 표준 라이브러리로 안 되면 설계를 바꿀 것
2. 코드를 고치면 `selftest.py` 를 돌릴 것. 검사가 없으면 추가할 것
3. 이 컨테이너는 외부 뉴스 사이트 접속이 막혀 있다. 수집 코드는
   고정 샘플로 검증하고, 실제 피드는 Actions에서 확인한다
4. 운영자에게 보고할 때 한국어로, 숫자를 부풀리지 말 것
